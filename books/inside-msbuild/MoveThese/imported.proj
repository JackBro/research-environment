<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003"
         DefaultTargets="Build" 
         InitialTargets="PrepareBuild"
         >
  <Target Name="Build">
    <Message Text="Build called in imported.proj"/>
  </Target>
  <Target Name="PrepareBuild">
    <Message Text="PrepareBuild called in imported.proj"/>
  </Target>
</Project>